import 'vue-i18n';

// This empty file is intentional.
// By importing 'vue-i18n', we are simply telling TypeScript to include
// the global type augmentations provided by the library, which is
// enough to make `$t` available globally in the type system.
